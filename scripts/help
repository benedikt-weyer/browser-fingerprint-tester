#!/usr/bin/env bash
set -euo pipefail

cat <<'EOF'
Available commands:

  help          Show this list of available commands
  pnpm start    Start the dev server with hot reload (http://localhost:3000)
  pnpm install  Install project dependencies
EOF
